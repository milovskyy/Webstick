# Relative to prisma/ folder (where schema.prisma lives). Use ../prisma/prisma/dev.db so DB is at project/prisma/prisma/dev.db
DATABASE_URL="file:../prisma/prisma/dev.db"
REDIS_URL="redis://localhost:6379"
# Optional: for Docker on Windows if volumes fail — set to absolute path to project root (e.g. C:/projects/Webstick-testwork). Default: .
# PROJECT_DIR=.
