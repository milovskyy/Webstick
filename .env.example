# Docker Compose: скопіюй у .env і при потребі відредагуй.
# Windows: щоб контейнер бачив ту саму базу й фото що й npm run dev:

# Коректне перетворення шляхів Windows (обовʼязково для монтів)
COMPOSE_CONVERT_WINDOWS_PATHS=1

# DATABASE_URL для SQLite (шлях відносно кореня проекту; у проєкті БД зараз у prisma/prisma/dev.db).
# DATABASE_URL="file:./prisma/prisma/dev.db"

# Явний шлях до кореня проекту (де лежать prisma/ та public/uploads).
# Якщо не вказати — використовується поточна папка при запуску docker-compose.
# Приклад (подстав свій шлях):
# PROJECT_DIR=D:/1_IT/Pratice/Next.js/Webstick-testwork
